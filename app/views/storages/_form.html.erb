<%= form_for @storage do |form| %>
  <!-- <% if @storage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@storage.errors.count, "error") %> prohibited this storage from being saved:</h2>

      <ul>
      <% @storage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %> -->


  <div class="field">
    <%= form.label :storage_name %><br>
    <%= form.text_area :storage_name, id: :storage_name %>
  </div>
  <br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
